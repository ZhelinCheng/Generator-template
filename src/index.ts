/**
 * Created by ChengZheLin on 2019/5/14.
 * Features: index
 */

import download from './lib/download'
import unzip from 'unzip'
import fs from 'fs'
import path from 'path'

(async function () {
  /*await download({
    label: '测试',
    url: 'https://github.com/ZhelinCheng/webpack-pages/archive/master.zip'
  })*/

  /*let readStream = fs.createReadStream(path.resolve(__dirname, '../doodle.zip'));
  readStream.pipe(unzip.Extract({ path: 'output' }))*/
}())


